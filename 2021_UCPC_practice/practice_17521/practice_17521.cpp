#include <iostream>
using namespace std;

// Byte Coin

int main()
{
	int n, w; // 요일 수, 초기 현금
	int i, money = 0;

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> n >> w;

	for (int i = 0; i <= n; i++)
	{
		cin >> i;

		if ()
	}


	return 0;
}